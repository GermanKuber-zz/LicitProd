<UserControl
    x:Class="LicitProd.UI.Uwp.UserControls.ContentPanelUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LicitProd.UI.Uwp.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entities="using:LicitProd.Entities"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}"  Text="{x:Bind Title}" Margin="10" ></TextBlock>
        <StackPanel BorderBrush="LightGray" BorderThickness="1" Margin="10">
            <ContentPresenter Content="{x:Bind MainContent}" />
        </StackPanel>
    </StackPanel>

</UserControl>
